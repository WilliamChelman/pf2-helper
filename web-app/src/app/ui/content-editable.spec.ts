import { ContentEditable } from './content-editable.util';

describe('ContentEditable', () => {
  it('should create an instance', () => {
    const directive = new ContentEditable();
    expect(directive).toBeTruthy();
  });
});
