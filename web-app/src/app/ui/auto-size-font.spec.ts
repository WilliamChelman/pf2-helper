import { AutoSizeFont } from './auto-size-font';

describe('AutoSizeFont', () => {
  it('should create an instance', () => {
    const directive = new AutoSizeFont();
    expect(directive).toBeTruthy();
  });
});
